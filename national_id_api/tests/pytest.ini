[pytest]
minversion = 6.0
addopts = --cov=app --cov-report=term --cov-report=html --cov-report=xml
asyncio_mode = auto
testpaths = 
    tests
python_files = test_*.py
markers =
    asyncio: mark test as asyncio